import styled from '@emotion/styled';

const DarkModeSwitch = () => {
  return <Container>DarkModeSwitch</Container>;
};

export default DarkModeSwitch;

const Container = styled.div`
  border: 1px solid;
`;
